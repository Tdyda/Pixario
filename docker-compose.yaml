services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    volumes:
      - ./api:/var/www/html
      - vendor:/var/www/html/vendor
    depends_on:
      - mysql
    expose:
      - 9000
    environment:
      DATABASE_URL: "mysql://symfony:symfony@mysql:3306/symfony"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    expose:
      - 80

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./api:/var/www/html
      - ./api/docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot_www:/var/www/certbot
      - certbot_conf:/etc/letsencrypt
    depends_on:
      - api
      - frontend

  certbot:
    image: certbot/certbot
    volumes:
      - certbot_www:/var/www/certbot
      - certbot_conf:/etc/letsencrypt

  mysql:
    image: mariadb:10.6
    environment:
      MYSQL_DATABASE: symfony
      MYSQL_USER: symfony
      MYSQL_PASSWORD: symfony
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3308:3306"
    volumes:
      - db_data:/var/lib/mysql

  mercure:
    image: dunglas/mercure
    restart: unless-stopped
    environment:
      SERVER_NAME: ":80"
      MERCURE_PUBLISHER_JWT_KEY: "W8YhawRXY62qDmU90OU6k6+rk38vdGANaqocPbHdDSA="
      MERCURE_SUBSCRIBER_JWT_KEY: "W8YhawRXY62qDmU90OU6k6+rk38vdGANaqocPbHdDSA="
      MERCURE_EXTRA_DIRECTIVES: |
        cors_origins http://pixario.pl

volumes:
  db_data:
  vendor:
  certbot_www:
  certbot_conf:
